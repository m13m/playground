module Mathematics(id2, {-neg,-} add, sub, mul) where


id2:: Int -> Int
id2 a = a

{-neg:: Num -> Num 
neg x = -(x)-}

add :: Int -> Int -> Int 
add x y = x + y

sub :: Int -> Int -> Int
sub x y = x - y

mul :: Int -> Int -> Int
mul x y = x * y

